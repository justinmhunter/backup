[user]
    name = Justin M. Hunter
    email = justinmhunter@gmail.com
[core]
    editor = vim
    whitespace = fix,-indent-with-non-tab,trailing-space,cr-at-eol
[web]
    browser = google-chrome
[credential]
    helper =
[color]
    ui = auto
[color "branch"]
    current = yellow bold
    local = green bold
    remote = cyan bold
[color "diff"]
    meta = yellow bold
    frag = magenta bold
    old = red bold
    new = green bold
    whitespace = red reverse
[color "status"]
    added = green bold
    changed = yellow bold
    untracked = red bold
[alias]
    br = branch
    bd = branch -D
    co = checkout
    com = checkout master
    st = status
    pushbranch = "!git push origin $(git rev-parse --abbrev-ref HEAD)"